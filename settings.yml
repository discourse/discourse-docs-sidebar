categories_toc_topics:
  type: objects
  default: []
  schema:
    name: category
    identifier: category
    properties:
      category:
        type: categories
        required: true
        validations:
          max: 1
      topic_id:
        type: integer
        required: true
